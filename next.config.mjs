// Inside your file

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // basePath: '/project_management',
  // assetPrefix: process?.env?.APP_URL || 'https://devenviro.aglprojects.co.in/project_management/',
};

export default nextConfig;
